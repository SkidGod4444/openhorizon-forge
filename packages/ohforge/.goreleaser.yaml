version: 2

project_name: ohctl

before:
  hooks:
    - go mod tidy

builds:
  - id: ohctl
    binary: ohctl
    main: ./cmd/ohctl
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: ohctl-archive
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md

checksum:
  name_template: checksums.txt

release:
  github:
    owner: openhorizon
    name: openhorizon-forge
